# Hotswitch configuration
# Same file works for both sender (macOS) and receiver (Windows)

[sender]
target = "10.0.0.XXX:24801"  # Gaming PC IP:port
hotkey = "ctrl+escape"         # Toggle capture

[receiver]
listen = "0.0.0.0:24801"      # Listen on all interfaces
